services:
  - type: web
    name: text-extraction-flask
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python flask_app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.3
